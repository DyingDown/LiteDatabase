namespace LiteDatabase.Storage;

public interface IStorageEngine {
    // 页面管理
    // PageId AllocatePage();
    // void FreePage(PageId pageId);
    // Page ReadPage(PageId pageId);
    // void WritePage(PageId pageId, Page page);
    
    // 记录管理
    // RecordId InsertRecord(PageId pageId, byte[] recordData);
    // byte[] ReadRecord(RecordId recordId);
    // void UpdateRecord(RecordId recordId, byte[] recordData);
    // void DeleteRecord(RecordId recordId);
}